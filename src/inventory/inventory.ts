import Resource from "./resource";

export default class Inventory {
    private readonly resource = new Map<Resource, number>();

    public canAddResource(resource: Resource, quantity: number) {
        
    }
}